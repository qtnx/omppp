CodeGraph is ready for this workspace. Use `codegraph_explore` first when you do not yet know where the code lives; its returned line-numbered source is already read, so do not re-read or re-grep it unless the result is incomplete or stale. Read or grep directly when you already know the exact path or symbol. If a query comes back off-target (unrelated modules, another worktree, files flagged "changed on disk"), the index does not cover that area: switch to `grep`/`read` for it instead of re-querying or rebuilding.
