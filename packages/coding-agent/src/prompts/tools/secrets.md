List the secrets available to this session. Values are injected as environment variables into bash commands; reference a secret as `$NAME`.

Plaintext is NEVER available. The tool returns names, masks, and lengths only.
