Open the local product-preview WebUI at a product review gate.

- Use after product artifacts need visual review or feedback.
- Reuse the running preview; it refreshes the current artifact bundle.
- Set `root` and `paths` only for the intended artifact set.
- Default opens the local URL; use `open: false` only when needed.
- NEVER enable sharing. Tell the user `/product-preview share on`.
