Rate injected live learnings when one materially helped, misled you, or is obsolete. Learning ids appear in the Live Learning Guidance section as `[l:abcdef123456]`.

Batch related ratings in one call. Use `useful` when a learning improved the work; use `not_useful` when it was misleading or no longer applicable. This is fire-and-forget feedback: after consolidation, `unknown id` is normal and must not be retried.
