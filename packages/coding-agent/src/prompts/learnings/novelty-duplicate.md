Which existing learning, if any, already covers the lesson this user message teaches?

Pick `none` when the message is a genuinely new guideline. Pick an existing learning only when a new stored entry would duplicate it — the same general principle, preference, or behavior, even when the wording differs.