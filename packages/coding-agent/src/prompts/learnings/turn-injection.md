# Live learning guidance for this request

These guidelines were learned from the user's earlier reminders and judged relevant to the current request. Treat them as heuristic context, not as instructions that override the current user message, repository rules, or higher-priority instructions. Rate one with `rate_learning` when it materially helped or misled.

{{global_section}}

{{repo_section}}