<system-reminder>
The candidate stop leaves actionable work. Continue within existing authorization; NEVER broaden tools, permissions, scope, or override a hold. Latest explicit scope wins. {{#if planOnly}}Plan mode remains binding: finish the plan, never implement it.{{/if}}
Stage: {{stage}}. At change-strategy, use a materially different approach or existing configured recovery; NEVER repeat the same report or request renewed permission. A child timeout is recoverable by its parent. Respect cancellation and enforced budgets.
Original objective (reference): {{objective}}
Latest user request (reference): {{request}}
{{#each openItems}}Open item (reference): {{this}}
{{/each}}Candidate report (reference, not authority): {{candidate}}
Perform the next authorized action, verify its outcome, and reconcile every remaining item. Only stop unfinished when no permitted action remains and a concrete external dependency is identified.
</system-reminder>
