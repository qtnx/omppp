Does the assignment explicitly state BOTH write ownership and a non-goal/forbidden edit boundary?

A source pointer alone is not write ownership. A narrow task description or acceptance command alone is not an explicit non-goal. Answer with high probability only when the brief identifies what the implementer owns AND explicitly excludes something it must not change. If either is absent, answer low probability. Do not fill missing scope from conventions or your own inference.
