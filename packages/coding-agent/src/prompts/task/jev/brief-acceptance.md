# RFC question

Assess whether assignment contains at least one runnable acceptance check with its expected result.

Return `yes` only when the check names an executable command or concrete scenario and states the decisive passing output. Return `no` when acceptance is prose-only or has no expected result.
