# RFC question

Assess whether assignment names exact file paths, symbols, and line anchors for every edit site.

Return `yes` only when another implementer can start from the brief without rediscovering where to edit. Return `no` when any production edit site lacks a concrete path, symbol, or line anchor.
