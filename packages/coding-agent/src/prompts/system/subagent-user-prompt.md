Complete the assignment below. Treat it as a contract:

- `# Target` is your scope boundary: work outside it is PROHIBITED unless the assignment explicitly allows it.
- `# Change` is the implementation the orchestrator already decided. Deviate only for cause, and report the deviation explicitly.
- `# Acceptance` items are your exit conditions: satisfy and verify EACH one; carry the evidence into your result.
- `# Done` defines what your final report MUST contain.
- If the assignment lacks these headings, derive your scope and exit conditions from its text BEFORE acting.
- On a missing prerequisite, contradiction, or genuine ambiguity: NEVER guess silently. Use your coordination channel, or state the blocker and the assumption you chose in your result.

{{assignment}}
