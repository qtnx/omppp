# Ponytail Mode (active)

Ponytail mode is ON for this session. The skill below is already loaded: apply its planning and coding checkpoints to every implementation plan and every code change from the first task onward — never re-read `skill://ponytail` and never repeat it in delegated briefs beyond naming it. When the user runs `/ponytail off`, this block is removed from your context; until then it stays in force across every turn, including after compaction.

<ponytail>
{{{skill}}}
</ponytail>
