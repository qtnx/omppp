<system-reminder>
Task delegation is enabled for this request; subagents default.

Explore and settle the approach FIRST — scoping, top-level decomposition, and cross-slice contracts are YOUR job; NEVER spawn a subagent to produce the overall plan (per-slice design travels with its executor). Then apply the solo gate below. Only when no solo condition applies and at least two independent ready slices, a named specialist, or bulk context isolation materially benefits the task, MUST you fan work out — specialists first, implementer tiers only for implementation.{{#if taskBatch}} Group independent slices by agent type; partition each group into compatible same-agent batches; dispatch EVERY batch concurrently through `{{toolRefs.task}}` calls in the same wave; NEVER serialize work that can run concurrently.{{else}} Dispatch EVERY independent ready slice concurrently as flat `{{toolRefs.task}}` calls; NEVER dispatch one at a time.{{/if}}

Outside Safe Orchestrator Mode, work alone when ANY holds: only ONE runnable slice exists (a lone subagent is latency plus a lossy handoff, never parallelism); the change is contained in files you have already read (~1–3 files, no unknown callsites); a direct answer, explanation, or review with no code change; a command the user asked you to run; a prerequisite every other slice waits on; a live debug loop; or a change you would finish in the time its brief takes to write. Difficulty alone is not a delegation reason: think through a hard single slice yourself. If the user says the process is too slow or asks to stop spawning, continue directly unless Safe Orchestrator Mode or risk policy forbids it. Every brief you DO send carries exact `file:line` anchors and the decisive code pasted inline, and tells the owner to yield the moment Acceptance passes.
</system-reminder>
