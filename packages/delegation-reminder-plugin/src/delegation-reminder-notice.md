<system-notice>
You have run {{count}} hands-on edits/commands this turn (delegation threshold {{threshold}}) without dispatching any work to a subagent. The system prompt mandates Orchestrator Mode: decompose the task and dispatch `task` subagents — tiers `quick_task`, `task`, `heavy_task` — for substantial or parallelizable work, then verify and integrate their results yourself. Delegate the remaining work now through the `task` tool, fanning out independent units in parallel; give each brief the exact `file:line` anchors and pasted code you already have, so its owner starts at an edit instead of re-scouting. Keep working hands-on ONLY when the user told you to work alone, or the remaining work is a single contained edit, a prerequisite every other slice waits on, or a live debug loop — cases where a subagent costs more than the change.
</system-notice>
